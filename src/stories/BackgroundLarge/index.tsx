import React from 'react';

import styles from './index.module.scss';

export const BackgroundLarge: React.FC = () => (
  <div className={styles.background}>
    <div className={styles.photo_container} />
  </div>
);
